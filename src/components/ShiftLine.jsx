import '../styles/ShiftLine.css'

export default function ShiftLine() {
  return (
    <section className="shift" id="shift">
      <div className="shift-inner">
        <p className="shift-line">
          Anyone can build a website now.{' '}
          <span className="shift-em">I build what your business actually needs to grow.</span>
        </p>
        <p className="shift-sub">
          AI made websites a commodity. The hard part is knowing what actually
          moves a business — what to build, what to skip, what to run for you.
          That's the part I own.
        </p>
      </div>
    </section>
  )
}
